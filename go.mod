// Single Go module for the whole origin. Many backing-CLI binaries are built
// off cmd/<tool>/ and versioned independently via release-please (§1, §8b).
// Rename this path to your org's repo after creating from the template.
module github.com/my-org/my-skills

go 1.23
